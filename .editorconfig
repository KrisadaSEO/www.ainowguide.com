root = true
# Convention: no em dashes (—). Use "..." or "-" instead.
# Enforced by .githooks/pre-commit — activate with: git config core.hooksPath .githooks

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false
